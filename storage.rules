rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Ścieżka zgodna z StorageService: reports/{uid}/...
    match /reports/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
